# 任务：生成 XX 词库的全部标准答案

## 你的任务

为词库 `XX` 生成所有词条的标准答案。每个词条一个 markdown 文件，写入 `D:\openclaw\workspace\quickask-clone\answers\词条名.md`。

## 风格样本（必须严格遵守）

参考 `D:\openclaw\workspace\quickask-clone\answers\墨菲定律.md` 和 `D:\openclaw\workspace\quickask-clone\answers\最小可行产品.md`。

每个文件结构：

```markdown
# 词条名

> 标准答案（约 200 字）

[标准答案正文，180-220 字]

---

**结构拆解**：
- 定义（30 字）：一句话讲清楚是什么
- 解释（80 字）：补充背景和内涵
- 例子（50 字）：用 1-2 个生活化场景
- 升华（40 字）：落到行动或感悟

**60 秒演讲模板**：
> "说到 词条名，我第一反应是……（举例子）……这告诉我们一个朴素的道理……（升华）今天我想说的是……（个人案例/感悟）"
```

## 标准答案正文要求（最关键）

1. **字数**：180-220 字（中文字符数，不含标点）
2. **结构**：
   - 定义（30 字）：用通俗语言一句话讲清楚是什么
   - 解释（80 字）：补充背景、内涵、来源
   - 例子（50 字）：用 1-2 个生活化场景，让人秒懂
   - 升华（40 字）：落到行动或感悟，对听众有用
3. **风格**：
   - 通俗、口语化，像聊天不是论文
   - 用"我""你""我们"等代入感强的代词
   - 避免学术腔，避免堆术语
   - **不要写公式、推导过程**
   - 例子优先生活场景 > 商业案例
   - 适合姐姐（26岁程序员）讲述给职场/面试听众
4. **价值**：
   - 每条要让听的人"哦原来是这样"或"我学到了"
   - 结尾要落到"所以我们该怎么做"

## 词库清单（从 index.html 提取）

请打开 `D:\openclaw\workspace\quickask-clone\index.html`，找到 `id: 'XX'` 那一项，读取 topics 数组里所有词条。每个词条格式是 `词条名 | 30字摘要`。

## 文件命名规范

- 文件名 = 词条名（去掉 " | 摘要" 部分）
- 路径：`D:\openclaw\workspace\quickask-clone\answers\词条名.md`
- 词条名包含特殊字符（如 `/` `:` `*`）时，替换为下划线 `_`
  - 例如 "A/B 测试" → "A_B测试.md"
  - 例如 "PDCA 循环" → "PDCA 循环.md"（含空格和字母保留）
- 文件已存在则覆盖

## 工作流程

1. 读取 `D:\openclaw\workspace\quickask-clone\index.html`，提取 `id: 'XX'` 的所有 topics
2. 对每个词条：
   - 用 web_search 或知识库确认专业准确性
   - 写标准答案（180-220 字）
   - 写入对应文件
3. 完成后返回：
   - 写了几个文件
   - 失败的词条（如果有过）
   - 字数超标的文件（如果有）

## 重要

- **不要做任何 UI 改动**，只生成 markdown 文件
- **不要碰 index.html**
- **不要碰其他词库的答案文件**（即使你觉得某个词更适合放别处）
- 写答案时若发现词条太偏/不熟/争议，标出来让 O 米决定
- 单个文件写失败不能阻塞其他文件，跳过继续

## 验证脚本（写完运行一遍）

```bash
cd D:\openclaw\workspace\quickask-clone
node -e "
const fs = require('fs');
const path = require('path');
const dir = path.join(__dirname, 'answers');
const files = fs.readdirSync(dir).filter(f => f.endsWith('.md'));
console.log('答案文件总数:', files.length);
"
```

期望数量 = `id: 'XX'` 的 topics 数组长度。

开始吧。完成后汇报。