# Speech 手机使用教程 🍚

## 📱 姐姐手机上怎么用

### 方案 A：手机浏览器直接用（最简单）

1. 把 `D:\openclaw\workspace\quickask-clone\index.html` 这一个文件复制到手机（微信/邮件/网盘/USB 都行）
2. 用 Safari（iPhone）或 Chrome（安卓）打开
3. 立即可用！

⚠️ 这个方案缺点：没网时打开需要从 CDN 加载 React，所以建议先打开一次让它缓存。

### 方案 B：加到主屏幕当 App 用（推荐）

#### iPhone（Safari）：

1. 把 Speech 项目文件夹传到 iPhone（推荐用 AirDrop，或者传到一个 iCloud Drive / 网盘目录）
2. Safari 打开 `index.html`
3. 点底部分享按钮（带向上箭头的方框）
4. 选「添加到主屏幕」
5. 改个名字（默认就是 Speech）→ 点「添加」
6. 主屏幕就出现一个橙色「S」图标，点开就是全屏 App 了！

#### 安卓（Chrome）：

1. 把项目文件夹传到手机
2. Chrome 打开 `index.html`
3. 右上角菜单 → 「添加到主屏幕」
4. 确认名字 → 「添加」
5. 主屏幕出现图标，点开就是 App

### 方案 C：O 米帮你部署到公网（最省心）

如果你想让手机不用传文件就能开，O 米可以：
- 部署到 GitHub Pages（免费，但国内访问慢）
- 部署到 Vercel（免费，国内访问一般）
- 部署到国内云服务器（10-20元/月，速度快）

姐姐要哪种？告诉 O 米一声

## ✨ 已做的手机适配

✅ 响应式布局（<720px 全面优化）
✅ 触摸友好（按钮 ≥48px）
✅ 防止双击放大 / 长按选中
✅ iOS 刘海屏安全区适配
✅ 输入框字号 16px 防止点击放大
✅ 主题色（橙色 #d97757，和桌面一致）
✅ PWA 离线缓存（首次打开后无网可用）
✅ PWA 主屏幕图标（橙色「S」）
✅ 全屏 App 模式（隐藏浏览器地址栏）
✅ 字体本地兜底（PingFang SC / 微软雅黑）
✅ 7 档梗评分（夯爆了~拉中拉之拉一裤兜）

## 🆘 常见问题

**Q: 没网能用吗？**
A: 首次打开需要联网加载 React（CDN），之后 Service Worker 缓存好就完全离线可用。

**Q: 数据会丢吗？**
A: 复盘记录存在 localStorage 里，换浏览器/清缓存会丢。重要的复盘记得截图。

**Q: 同步到电脑？**
A: localStorage 是按域名隔离的，加到主屏幕后是个独立的 App 域，不会自动同步。要同步得用云端方案。

**Q: 字体显示不出中文字体？**
A: 已用 PingFang SC（iPhone 默认）+ 微软雅黑兜底，应该都正常。